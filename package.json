{
  "name": "dev-portfolio",
  "version": "1.0.0",
  "description": "Portfolio Frontend - Dark Cyber Theme",
  "private": true,
  "scripts": {
    "install": "cd client && npm install",
    "start": "cd client && npm start",
    "build": "cd client && npm run build"
  }
}
